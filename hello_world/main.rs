// Prints "Hello, world!" in the terminal.
// Main function, the first function to run in an executable rust program.
fn main() {
    // println! is a macro, and not a function.
    println!("Hello, world!");
}